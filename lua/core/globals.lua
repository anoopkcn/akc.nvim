Print = function(v)
	-- given a table hash print the table
	print(vim.inspect(v))
	return v
end

Lookup = function(string)
	-- given a string open browser and look up the string
	-- using the default search engine
	-- local search_engine = "https://www.google.com/search?q="
end
